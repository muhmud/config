#!/bin/bash

EXISTING_PROCESS=$(pgrep -f -o 'suppress-screensaver')
MY_PROCESS=$(echo $$)

if [[ "$MY_PROCESS" == "$EXISTING_PROCESS" ]]; then
  while true
  do
      xscreensaver-command --deactivate > /dev/null 2>&1

      sleep 60;
  done
fi;
